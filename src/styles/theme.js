import { createTheme } from "@mui/material";

export const themeOptions = createTheme({
    palette: {
        type: 'light',
        primary: {
            main: '#6e449d',
        },
        secondary: {
            main: '#b093ca',
        },
    },
})